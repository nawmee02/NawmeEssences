# Local Supabase credentials — never commit this file

# Generated image variants (reproducible from originals)
assets/images/generated/

# Upload manifest — local record of what's in Storage
assets/images/.upload-manifest.json

# Generated product + brand pages — built in CI from Supabase, not committed
product/
brands/

# Node
node_modules/

# Secrets — never commit
.env
